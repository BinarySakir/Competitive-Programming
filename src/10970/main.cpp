#include <bits/stdc++.h>

using namespace std;

int main(){

    int x, y, split_count;
    while(cin >> x >> y){
        split_count = (x*y)-1;
        cout << split_count << endl;
    }
    return 0;

}
